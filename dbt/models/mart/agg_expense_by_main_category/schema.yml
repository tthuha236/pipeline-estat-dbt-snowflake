version: 2
models:
  - name: agg_expense_by_main_category
    config:
      materialized: incremental
      incremental_strategy: merge
      unique_key: ['ken_name','year','month','main_category_name']
      description: "Aggregated fact_expense table at the sub expense category level"